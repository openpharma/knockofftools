# Internal check of whether class of response is compatible with family

Do not call this function on its own

## Usage

``` r
check_family(y, type)
```

## Arguments

- y:

  response variable

- type:

  "regression", "classification", or "survival"

## Value

this function doesn't return anything
